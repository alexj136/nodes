    ! receive $addone : n_rch .
    let n   = <- n_rch .
    let rch = -> n_rch .
    send rch : n + 1 .
    end

|

    new rch .
    send $addone : { 10 , rch } .
    receive rch : n .
    send $resultis : n .
    end
