new f: @{;int}.
new print: @{k; k}.
[
    server f;; a: int.
    if a > 0 then
        send print; int; a - 1.
        send f;; a - 1.
        end
    else
        end
    endif

|

    send f;; 10.
    end
]
