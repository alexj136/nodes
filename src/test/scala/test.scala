package test
/*
import syntax._
import interpreter._
import org.scalacheck.Properties
import org.scalacheck.Prop.forAll

object InterpreterProperties extends Properies("Proc") {

  val names: Map[Name, String] = Map
    ( ( new Name(0), "a" )
    , ( new Name(1), "b" )
    )
  val next: Name = new Name(2)

  property("sumall") = {
  }
}*/
